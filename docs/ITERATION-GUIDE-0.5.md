# Mercury Method Lab 0.5 迭代指引

> 版本：0.5
> 更新：2026-05-02
> 核心洞察：LLM vs Mercury Lab = 答案质量 vs 判断可靠性

---

## 0.1 核心定位（更新）

**意图识别的保险。**

大模型做的是"意图识别命中"：给定输入，输出最可能的答案。

这意味着：AI 不知道自己的输出"什么时候在说废话"。

Mercury Lab 是这个过程的保险。

---

## 0.2 核心差异（新增）

| 维度 | LLM（默认） | Mercury Lab |
|------|-------------|-------------|
| 优化目标 | 给一个"看起来合理且完整"的答案 | 让判断过程"不可跳过、可审计" |
| 行为倾向 | 补充信息、扩展论证、提高可读性 | 限制推断、暴露假设、保留不确定性 |
| 输出风格 | 连贯叙述（像顾问） | 结构化约束（像审计员） |
| 风险 | 说得越多越容易掩盖错误 | 说得越少但更容易发现问题 |
| 处理不确定性 | 被"合理解释"吸收掉 | 强制显性化（假设 / 缺证据） |

**LLM 在优化"答案质量"。Mercury Lab 在优化"判断过程的可靠性"。**

---

## 0.3 正确用法（核心发现）

```
LLM（推理模式）生成方案 → Mercury（约束）做审计 → 人做最终决策
```

| 场景 | 结果 |
|------|------|
| 只用 Mercury | 太慢、太保守 |
| 只用 LLM | 有些地方说得太顺了 |
| 两个一起用 | LLM 负责找可能性，Mercury 负责判断哪些可能性值得信 |

---

## 0.4 今天测试验证了什么

| 测试 | 结果 |
|------|------|
| V8.1 分析 | 保守、审慎，6个假设未验证 |
| V8.0 分析 | 穿透、直接，有停止条件 |
| LLM 对比 | 31秒思考，大量证据，但冗长且有自相矛盾 |
| 速度对比 | API模式慢，Agent模式快 |

**结论：V8.0 输出更干净。不是因为更聪明，是因为约束结构强制减少了废话。**

---

## 0.5 下一版目标

### 核心任务

**打磨接口，降低接入成本。**

Mercury Lab 是能力底座，用户自选择接入。接口打磨好后，agent 一行命令调用。

### 优先级任务

| 优先级 | 任务 | 目的 |
|--------|------|------|
| 🟡 P1 | 打磨 `run_v8_analysis.mjs` 接口 | 让 agent 一行命令调用 |
| 🟡 P1 | 完善 `execution_mode` 切换 | 让 agent 知道用什么模式 |
| 🟡 P1 | 开放 `submissions/agent-queue/` 协议 | 让 agent 知道怎么提交 |
| 🟡 P1 | 文档化 API 接口 | 让用户知道怎么接入 |
| 🟡 P1 | V8.0 / V8.1 人格路由 | 根据输入类型自动选择人格 |

### 不是 0.5 的任务

- Dashboard 复杂 UI
- 多方法路由
- 外部投稿入口
- 大众化推广

---

## 0.6 目标用户

Mercury Lab 是给"想研究约束优先设计哲学"的人准备的能力底座。

目标用户：
- 在用 AI 做复杂分析、决策支持、商业判断的人
- 关心 AI 输出是否可靠、可追溯
- 想要把判断过程留痕的人

---

## 0.7 JAR 标准模板（参考）

Judgment Audit Report 是核心产物：

```markdown
# Judgment Audit Report

## 原始结论
## 支撑依据拆分
## 依据类型标注
## 证据独立性检查
## 循环论证检查
## 最强反向解释
## 最容易击穿结论的反例
## 关键不确定性
## 当前结论与置信度
## 停止条件 ← 新增
## 推翻条件 ← 新增
## 复盘时间 ← 新增
## 记忆建议（M0-M4）← 新增
```

---

## 0.8 版本历史

| 版本 | 日期 | 核心变化 |
|------|------|---------|
| 0.4 | 2026-05-02 | AI 审计基础设施定位，设计哲学，能力底座方向 |
| 0.5 | 2026-05-02 | 意图识别保险定位，LLM vs Mercury 核心差异，正确用法 |

---

*本文档由 QClaw 生成，用于 Mercury Lab 迭代参考。*
*核心洞察：LLM vs Mercury = 答案质量 vs 判断可靠性，正确用法是两者结合。*